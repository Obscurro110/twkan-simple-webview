# v1.2.0

## 本次更新

- 修复"滑到底部点一下就跳到广告"的问题。原因有两处：网站页脚和广告锚点紧贴在正文下方，而应用之前对所有非 `twkan.com` 的链接都会直接调起外部浏览器，所以轻微误碰就会跳出去。
- 新增外链点击拦截：阅读页中所有指向站外的链接点击都会被直接吞掉，不再跳出应用。阅读设置面板、站内章节链接、网站自带的简繁切换按钮均不受影响。
- 底部广告清理增强：自动移除大面积、无文字或悬浮定位的站外广告锚点，这类元素最容易在翻到章节末尾时被误碰。
- 无限滚动启用后，自动隐藏正文下方的页脚、友情链接、Cookie 声明、DMCA 等外链区块，章节末尾不会再有外跳链接压在手指下方。
- "正在加载下一章…"和"点这里重试/验证"提示改为独立的高层级点击区块，带圆角背景和边距，不会再和相邻广告挤在一起误触。
- Android 端补上兜底拦截：已知广告域名以及 `intent:`/`market:` 这类应用商店跳转会被静默丢弃，不再调起浏览器或应用商店。`mailto:`/`tel:`/`sms:` 等正常协议保持可用。
- 修复重复注入脚本时会叠加多个广告清理定时器的问题。
- v1.1.9 的 Cloudflare 请求节流、阅读记录入口、点击正文收起设置面板等行为均保持不变。

## 使用说明

如果确实需要打开某个站外链接，可以先在阅读设置里跳转到网站页面再操作，或使用系统浏览器访问。阅读页内的外链点击会被有意忽略，用来避免误触广告。

## 安装

直接下载下方对应 APK 安装即可。Debug 和 Release 使用相同签名，可直接覆盖安装，不需要先卸载旧版本。

- `debug`：用于测试，保留调试构建标识。
- `release`：正式使用，推荐下载。
