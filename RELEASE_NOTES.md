# v1.1.5

## 本次更新

- 修复下一章后台 `fetch` 收到 Cloudflare 挑战页或 HTTP 403/429 后只显示“下一章加载失败”的问题。
- 遇到 Cloudflare/403/429 时，改为由当前可见 WebView 打开目标章节，让网站在完整页面上下文中执行验证。
- 验证页面可以正常显示，完成验证后继续使用当前页面导航，不再反复触发后台请求。
- 保持上一版的请求优化：不自动预加载后续章节，不创建隐藏同步 WebView。
- 扩展 Cloudflare 挑战页识别，覆盖常见的浏览器检查提示。

## 使用说明

Cloudflare 验证由网站控制，应用不能绕过验证。遇到验证时，请在当前页面完成一次验证并等待页面加载完成；不要连续快速点击重试。完成后返回或继续打开下一章即可。

## 安装

直接下载下方对应 APK 安装即可。Debug 和 Release 使用相同签名，可直接覆盖安装，不需要先卸载旧版本。

- `debug`：用于测试，保留调试构建标识。
- `release`：正式使用，推荐下载。
