# v1.1.7

## 本次更新

- 找到"几乎每一章都要验证"的关键原因：后台预加载下一章用的是纯 `fetch()` 请求，它只会下载网页文本，**不会执行**返回的脚本；而 Cloudflare 大部分"非交互式"验证（显示"Just a moment…"几秒后自动放行）恰恰需要脚本真正在页面里跑起来才能拿到放行凭证，所以纯后台请求几乎必定被拦截。
- 新增静默兜底通道：当后台请求命中 Cloudflare 验证页或 403/429 时，不再立刻报错，而是用一个用户看不到的隐藏页面框架重新加载同一章节地址。隐藏框架是真实的页面环境，会自动执行验证脚本，大多数非交互式验证会在几秒内静默通过并拿到内容，用户完全无感知，无限滚动可以继续。
- 隐藏框架加了安全限制，只允许脚本运行和读取本站会话，但禁止验证页面把整个应用主界面强制跳转出去，避免误跳转破坏当前阅读页面。
- 仅当验证确实需要人工滑块/图形验证（隐藏框架内 12 秒左右仍未通过）时，才回退到原有流程：停止自动重试，显示"网站要求验证，点这里打开下一章"提示，用户点击后才用可见页面完成验证。
- 阅读设置持久化、章节内阅读位置恢复、35% 阅读记录判定等 v1.1.6 已修复的行为均保持不变，未做改动。

## 使用说明

Cloudflare 由网站控制，应用无法真正绕过，只能尽量减少打断阅读的验证次数。多数验证现在会在后台自动完成，无需操作；如仍看到"点这里打开下一章"提示，说明该次验证需要人工完成，请点击后在打开的页面完成验证，应用会自动恢复到之前保存的阅读位置和样式设置。

## 安装

直接下载下方对应 APK 安装即可。Debug 和 Release 使用相同签名，可直接覆盖安装，不需要先卸载旧版本。

- `debug`：用于测试，保留调试构建标识。
- `release`：正式使用，推荐下载。
