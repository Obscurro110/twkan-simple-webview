# v1.1.8

## 本次更新

- 阅读设置面板新增“阅读记录”按钮：点击即可直接跳转到网站自带的阅读记录页面（`https://twkan.com/history`），跳转前会自动保存当前的排版设置和阅读进度，方便之后返回继续阅读。
- 阅读设置面板打开后，只要点击面板以外的任意区域（比如小说正文），面板会自动收起、恢复成右下角的“Aa”小图标，不再遮挡阅读内容；点击面板内部（调整字体、行距等）不会触发收起。

## 使用说明

阅读设置面板右下角常驻一个“Aa”图标，点击展开可调整字体大小、行距、字间距、背景色，以及跳转到阅读记录或恢复默认设置；点击图标本身、面板右上角的“×”，或面板以外的任意位置都可以收起面板。

## 安装

直接下载下方对应 APK 安装即可。Debug 和 Release 使用相同签名，可直接覆盖安装，不需要先卸载旧版本。

- `debug`：用于测试，保留调试构建标识。
- `release`：正式使用，推荐下载。
