# v1.2.3

## 本次更新

- 修复「阅读记录」不更新：当章节实际进入约 35% 阅读线后，阅读器会将章节标题、链接和阅读时间写入应用本地记录。
- 阅读记录不会把预读、已追加但尚未读到的章节标记为已读；只有阅读线确认进入章节后才会新增或更新记录。
- 「阅读设置」中的「阅读记录」继续打开 `twkan.com/history`，并在该页面顶部展示应用维护的本地记录；每条记录可直接点开对应章节。
- 记录同时写入同源 Web Storage 与 Android `SharedPreferences`，提高页面重载、网站本地存储变化后的保留能力，最多保留最近 100 条。
- 移除已失效的阅读记录同步桥接。旧机制依赖第二个隐藏 WebView 加载章节以触发网站脚本，既会制造额外请求，也可能再次触发网站验证，因此不再使用。

## 保持不变

无限滚动、章节内阅读位置恢复、约 35% 阅读线判定、阅读设置持久化、短章节和非标准章节兼容、底部误触广告防护、可见页面验证流程均未改动。

## 使用说明

twkan.com 的阅读记录页面明确说明其记录保存在浏览器本地。应用现在在同一 WebView 会话中维护自己的本地阅读记录，不会额外加载隐藏章节页面，也不会尝试绕过 Cloudflare 验证。阅读章节时请让内容经过约 35% 的阅读线，然后从阅读设置打开「阅读记录」查看和继续阅读。

## 安装

直接下载下方对应 APK 安装即可。Debug 和 Release 使用相同签名，可直接覆盖安装，不需要先卸载旧版本。

- `debug`：用于测试，保留调试构建标识。
- `release`：正式使用，推荐下载。
