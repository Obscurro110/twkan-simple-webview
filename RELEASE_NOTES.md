# v1.2.2

## 本次更新

- 移除隐藏 iframe 的验证处理，不再在不可见页面中加载章节或尝试执行网站验证脚本。
- 后台加载下一章遇到明确的 Cloudflare 挑战页面或 `cf-mitigated: challenge` 响应头时，会停止自动加载并显示「网站要求验证，点这里打开下一章」。
- 点击验证提示后，下一章在当前顶层 WebView 中从开始到重定向完成都保持可见；若网站提供可交互验证，由用户直接完成。
- 可见验证页面不会注入阅读器脚本；页面完成验证并跳转回正常章节后，阅读器才继续初始化。
- `429 Too Many Requests` 不再被误判为验证：会读取 `Retry-After`，或按 30 秒默认冷却提示稍后重试，最长冷却 5 分钟。
- 恢复 Android System WebView 默认 User-Agent，不再移除 `wv` 标记，避免声明的浏览器身份与实际 WebView 环境不一致。
- 在可见验证完成后的正常页面、以及应用进入后台时调用 `CookieManager.flush()`，尽快持久化网站在同一 WebView 会话中写入的 Cookie。

## 保持不变

无限滚动、章节内阅读位置恢复、35% 阅读线判定、阅读设置持久化、短章节和非标准章节兼容、底部误触广告防护均未改动。

## 使用说明

Cloudflare 验证由网站控制，应用无法绕过。出现「网站要求验证，点这里打开下一章」时，请点击提示并在当前页面按网站要求完成验证。完成后，网站 Cookie 继续留在同一 WebView 会话中，阅读器会在正常章节页面恢复样式和阅读位置。

## 安装

直接下载下方对应 APK 安装即可。Debug 和 Release 使用相同签名，可直接覆盖安装，不需要先卸载旧版本。

- `debug`：用于测试，保留调试构建标识。
- `release`：正式使用，推荐下载。
