# v1.2.1

## 本次更新

- 改用「是否持有 Cloudflare 通行凭证」来决定后台加载方式，替换原来「最近 15 分钟是否遇到过验证」的粗略时间判断。凭证由 Android 端读取（该凭证禁止网页脚本读取），只向阅读逻辑返回“有/无”，不会把凭证内容暴露给页面。
- 持有有效凭证时，下一章直接走普通后台请求，速度更快，不再因为之前某次验证失败而长时间使用较慢的通道。
- 没有凭证且近期确实遇到过验证时，直接使用可执行网页脚本的真实页面通道，省掉一次注定会被验证页拦住的请求，每章由两次访问降为一次。
- 网站当前未启用验证时（本来就不会下发凭证）仍走普通请求，不会误判成需要验证。
- 加载成功后会重新确认凭证状态：普通请求成功即恢复常规模式；靠真实页面通道成功但仍未拿到凭证时，保持该通道，避免下一章又被验证拦住、反复消耗两次请求。
- 凭证状态查询结果有 20 秒缓存，连续翻章不会频繁跨层查询。

## 保持不变

无限滚动、章节内阅读位置恢复、35% 阅读进度判定、阅读设置持久化、底部误触广告防护均未改动。

## 使用说明

Cloudflare 由网站控制，应用无法真正绕过，本次优化只是减少应用自身产生的重复请求。若仍出现「网站要求验证，点这里打开下一章」，说明该次验证需要人工完成（滑块或图形验证），请点击后在打开的页面完成验证，应用会自动恢复之前的阅读位置和样式设置。

## 安装

直接下载下方对应 APK 安装即可。Debug 和 Release 使用相同签名，可直接覆盖安装，不需要先卸载旧版本。

- `debug`：用于测试，保留调试构建标识。
- `release`：正式使用，推荐下载。
