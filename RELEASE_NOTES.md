# v1.2.8

## 本次更新

- **修复偶发自行滑动**：阅读线每次越过章节边界时会调用 `history.replaceState()` 更新地址栏 URL，Android WebView 会对 URL 变化自动恢复该 URL 对应的历史滚动位置，导致偶发向上跳动。现在在阅读器初始化时设置 `history.scrollRestoration = 'manual'`，禁用浏览器自动滚动恢复，滚动完全由阅读器管理。

本次改动以 v1.2.3 为基础，仅新增这一行修复，其余逻辑与 v1.2.3 完全一致。

## 保持不变

无限滚动、约 35% 阅读线判定、阅读记录、阅读位置与设置持久化、短章节和非标准章节兼容、底部误触广告防护、可见页面验证流程均未改动。

## 安装

直接下载下方对应 APK 安装即可。Debug 和 Release 使用相同签名，可直接覆盖安装，不需要先卸载旧版本。

- `debug`：用于测试，保留调试构建标识。
- `release`：正式使用，推荐下载。
