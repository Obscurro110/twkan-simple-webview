# v1.1.0

## 本次更新

- 修复未开启翻墙时页面无法访问后无法前进、后退的问题。
- 网络失败时显示应用内提示，可直接重试、后退、前进或返回首页。
- 修复部分章节标题重复显示的问题。
- 清理章节标题、正文开头和章节之间的异常大段空白。
- 清理空段落、连续换行、固定高度和无内容占位块。
- 支持请假条、公告、停更通知等短章节加载。
- 支持“下一页”“下页”“继续阅读”等下一章节链接文本。
- 修复阅读记录页面误显示小说正文的问题。
- 只有章节真正滚动到阅读线后才同步阅读记录，预加载章节不会提前标记为已读。

## 安装

直接下载下方对应 APK 安装即可。Debug 和 Release 使用相同签名，可直接覆盖安装，不需要先卸载旧版本。

- `debug`：用于测试，保留调试构建标识。
- `release`：正式使用，推荐下载。

后续版本会使用简洁的语义化版本号，例如 `v1.1.1`、`v1.2.0`，每个版本都会创建独立 Release，历史版本不会被覆盖。
