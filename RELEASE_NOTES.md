# v1.1.4

## 本次更新

- 减少 Cloudflare 重复验证：取消启动后自动预加载后续三章。
- 取消使用隐藏 WebView 重复打开已读章节，避免后台请求反复触发验证。
- 章节只在用户实际滚动到下一章时请求，降低并发和异常访问频率。
- 保留设备实际 WebView 的浏览器版本，只移除 `wv` 标记，避免伪造固定 Chrome 版本造成指纹不一致。
- 下一章请求遇到 Cloudflare 时停止自动重试，提示用户在当前可见页面完成验证后再继续。
- 完成验证后可点击加载提示继续阅读，不再进入后台验证循环。
- 阅读记录继续使用本地记录和当前页面事件，不影响 35% 阅读线同步逻辑。

## 使用说明

Cloudflare 验证由网站控制，应用不能绕过验证。首次遇到验证时，请在当前页面完成一次验证；验证通过后再继续阅读。请保持翻墙连接稳定，不要连续快速点击重试。

## 安装

直接下载下方对应 APK 安装即可。Debug 和 Release 使用相同签名，可直接覆盖安装，不需要先卸载旧版本。

- `debug`：用于测试，保留调试构建标识。
- `release`：正式使用，推荐下载。
