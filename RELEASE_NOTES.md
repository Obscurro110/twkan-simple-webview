# v1.2.6

## 本次更新

- 进一步减少页面自行滑动：哨兵元素和无限阅读容器均明确禁用浏览器滚动锚定，章节容器允许锚定，减少新章节插入时浏览器为保持锚点造成的页面位移。
- 阅读位置恢复的滚动阈值从 24px 放宽到 60px，避免微小抖动时仍执行 `scrollTo()`。
- 自动追加下一章的逻辑与触发条件未改动；只移除可能由浏览器内部滚动管理引起的视觉跳动。

## 保持不变

无限滚动、约 35% 阅读线判定、未读章节不记入阅读记录、阅读位置与设置持久化、短章节和非标准章节兼容、底部误触广告防护、可见页面验证流程均未改动。

## 使用说明

如果本次更新后仍偶发自行滑动，请反馈以下信息帮助定位：

1. 滑动是否只在"接近页面底部、即将加载下一章"时出现，还是完全随机时刻？
2. 滑动方向是向上还是向下？
3. 滑动幅度大约多少（例如"半屏""一整屏""只跳了一点点"）？

这些信息能帮助判断是浏览器滚动锚定、位置恢复还是其他原因。

## 安装

直接下载下方对应 APK 安装即可。Debug 和 Release 使用相同签名，可直接覆盖安装，不需要先卸载旧版本。

- `debug`：用于测试，保留调试构建标识。
- `release`：正式使用，推荐下载。
