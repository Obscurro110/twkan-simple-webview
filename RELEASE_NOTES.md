# v1.1.6

## 本次更新

- 修复 v1.1.4/v1.1.5 中阅读设置键缺失导致字体大小、行距、字间距和背景色重置为默认值的问题。
- 自动迁移已有网页阅读设置，并将设置同步备份到 Android 原生存储；Cloudflare 页面重载后仍会保留阅读样式。
- 保存当前章节的章节内阅读偏移，Cloudflare 验证或目标章节重载后恢复到原来的段落位置，不再直接从章节开头开始。
- 位置恢复仅在页面 URL 和章节 URL 都匹配时执行，不会把一个章节的阅读位置套用到其他章节，也不会提前触发 35% 阅读记录。
- 后台章节请求遇到 Cloudflare/403/429 时停止自动跳转，只显示可点击提示；用户确认后才由可见 WebView 打开下一章，减少验证循环。
- 记住短时间内被 Cloudflare 拦截的下一章，页面重建后不再立即重复后台请求。

## 使用说明

Cloudflare 验证由网站控制，应用不能绕过验证。遇到验证提示时，请点击提示打开下一章，在可见页面完成验证并等待章节加载；不要连续快速点击重试。验证成功后，应用会尽量恢复已保存的阅读位置和阅读设置。

## 安装

直接下载下方对应 APK 安装即可。Debug 和 Release 使用相同签名，可直接覆盖安装，不需要先卸载旧版本。

- `debug`：用于测试，保留调试构建标识。
- `release`：正式使用，推荐下载。
