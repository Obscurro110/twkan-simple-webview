# v1.3.1

## 本次更新

**彻底解决滑动闪烁问题 - 暂停滑动时的后台操作**

针对 v1.3.0 仍存在的滑动闪烁问题，找到真正的根本原因并彻底解决：

### 核心改进

**滑动检测与智能暂停机制**：
- 检测用户滑动状态（scroll / touchmove 事件）
- **滑动时自动暂停 MutationObserver**，不触发繁简转换
- **滑动时自动暂停广告清理**，不删除 DOM 元素
- 滑动停止后 150ms 自动恢复后台操作

### 为什么 v1.3.0 还闪烁？

v1.3.0 虽然添加了硬件加速和 GPU 合成，但问题在于：
1. **广告拦截器每 5 秒运行** → 删除 DOM 元素 → 触发布局重排（reflow）
2. **MutationObserver 持续监听** → 任何 DOM 变化都触发繁简转换 → 触发重绘（repaint）
3. 即使有 GPU 加速，**布局重排本身无法避免**，会导致滑动时的视觉抖动

### v1.3.1 的解决方案

**在滑动时完全暂停所有可能导致布局变化的操作**：
```javascript
// 滑动时
if (isScrolling) return;  // MutationObserver 跳过
if (!isScrolling) { runAdCleanupPass(); }  // 广告清理跳过
```

**技术原理**：
- 滑动时不修改 DOM → 无布局重排 → 无闪烁
- 滑动停止 150ms 后恢复 → 保证功能完整性
- 使用 `{ passive: true }` 监听器 → 不阻塞滑动性能

## 保持不变

所有现有功能均保持不变：
- v1.3.0 的硬件加速优化
- v1.2.9 的 Cloudflare 验证优化
- 无限滚动与智能节流
- 繁简转换
- 阅读记录与位置保存
- 广告拦截与误触防护

## 安装

直接下载下方对应 APK 安装即可。Debug 和 Release 使用相同签名，可直接覆盖安装，不需要先卸载旧版本。

- `debug`：用于测试，保留调试构建标识。
- `release`：正式使用，推荐下载。
