# v1.2.9

## 本次更新

**精细化优化请求策略，降低 Cloudflare 验证频率**

针对"APP 每章验证、电脑浏览器不验证"的问题，实现多层次自适应请求优化：

### 1. 智能重试机制
- 遇到 Cloudflare 挑战时不立即打断阅读，而是自动延迟重试
- 第一次重试：等待 2.5 秒后自动重新请求
- 第二次重试：再等待 1.5 秒重试
- 两次重试都失败后，才提示用户手动打开验证页面

### 2. 自适应请求节流
- 基础间隔：3 秒
- 连续成功加载章节时，每次递增 500ms，最高到 8 秒
- 遇到挑战或限流后，重置到基础间隔并额外缓冲 5 秒
- 模仿真人阅读：间隔随行为动态调整，而非匀速自动化请求

### 3. 验证完成缓冲
- 用户完成可见验证后，额外等待 1 秒再发起后台请求
- 给 Cloudflare 后端足够时间更新风控评分

### 4. 触发距离优化
- 从距离底部 900px 收紧到 300px
- 只在用户真正接近底部时才触发自动加载
- 更接近真人滚动到底部再点击下一章的行为

### 5. 请求头优化
- `fetch()` 请求增加明确的 `Referer` 头
- 模拟"用户在当前章节页面点击下一章链接"的请求上下文

## 设计思路

桌面浏览器很少触发验证，因为它的行为模式符合真人特征：
- 请求间隔不固定（有快有慢）
- 完整的页面导航和浏览器指纹
- 稳定的 Cookie 和会话上下文

APP 的无限滚动是后台 `fetch()` 请求，容易被 Cloudflare 识别为自动化流量。现在通过动态节流、智能重试、缓冲等待，让 APP 的请求模式更接近真人在桌面浏览器的使用习惯。

## 保持不变

无限滚动、约 35% 阅读线判定、阅读记录、阅读位置与设置持久化、短章节和非标准章节兼容、底部误触广告防护、可见页面验证流程均未改动。

## 安装

直接下载下方对应 APK 安装即可。Debug 和 Release 使用相同签名，可直接覆盖安装，不需要先卸载旧版本。

- `debug`：用于测试，保留调试构建标识。
- `release`：正式使用，推荐下载。
