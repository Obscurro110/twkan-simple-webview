# v1.1.3

## 本次更新

- 修复普通背景下文字颜色被网站样式覆盖，统一使用高对比深色文字。
- 深色背景使用浅色文字，提升阅读清晰度。
- 阅读内容区域使用不透明背景，修复背景色只覆盖部分区域的问题。
- 背景设置覆盖正文根节点、章节包装层和页面底色，但不强制改变网站顶部导航栏。
- 扩展跨多个段落的宣传语清理，覆盖“希望读者记一下我们的域名台湾小说网 → twkan.com”等变体。
- 支持数学斜体、特殊字体和下划线形式的 `twkan` 宣传文字清理。
- 阅读设置数值输入改为完成输入后提交，避免输入多位数字时被中途截断。
- 调整右下角阅读设置按钮为固定安全位置，距离右侧和底部保留稳定间距。

## 安装

直接下载下方对应 APK 安装即可。Debug 和 Release 使用相同签名，可直接覆盖安装，不需要先卸载旧版本。

- `debug`：用于测试，保留调试构建标识。
- `release`：正式使用，推荐下载。
