# v1.1.9

## 本次更新

- 优化无限滚动的自动追加策略：自动加载触发距离从约 1600 像素收紧到 900 像素，避免章节还没有真正读到就过早请求下一章。
- 增加自动追加间隔：连续短章节或公告页面不会在短时间内连续发起多次章节请求，自动请求之间至少间隔 3 秒；手动点击下一章不受此延迟影响。
- 优化 Cloudflare 挑战后的请求路径：最近遇到验证后，后续章节优先使用能够执行页面脚本的隐藏页面请求，跳过很可能再次返回挑战页的纯 `fetch()`，减少一次章节被重复请求的情况。
- 记录近期验证状态，在应用重新加载阅读页面后的短时间内继续使用更保守的请求方式，避免刚通过验证又立即恢复高频后台请求。
- 页面跳转、切换到后台或进入阅读记录时，会取消尚未执行的自动追加计时器，避免离开阅读页后继续请求章节。
- 保留无限滚动、35% 阅读记录判定、阅读设置持久化和人工验证入口。

## 使用说明

Cloudflare 验证由网站的风控策略决定，应用无法真正绕过需要真人完成的验证。本版本主要减少重复请求和短时间连续请求；如果当前网络/IP 被要求进行滑块或验证码验证，仍需要按页面提示手动完成。

## 安装

直接下载下方对应 APK 安装即可。Debug 和 Release 使用相同签名，可直接覆盖安装，不需要先卸载旧版本。

- `debug`：用于测试，保留调试构建标识。
- `release`：正式使用，推荐下载。
