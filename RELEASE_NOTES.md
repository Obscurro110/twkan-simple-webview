# v1.2.4

## 本次更新

- 修复阅读页偶发自行滑动：动态插入下一章的无限阅读容器不再参与浏览器滚动锚定，减少内容插入时 WebView 为保持锚点而造成的页面位移。
- 收紧阅读位置恢复：页面初始化后最多恢复一次，不再连续多次强制 `scrollTo()`。
- 用户触摸、滚轮、按下或键盘操作会立刻取消尚未执行的位置恢复，避免用户开始阅读后被旧位置拉回。
- 手动点击原站「下一章」仍会平滑跳到新章节；自动追加下一章不会主动调用滚动命令。

## 保持不变

无限滚动、约 35% 阅读线判定、未读章节不记入阅读记录、阅读位置与设置持久化、短章节和非标准章节兼容、底部误触广告防护、可见页面验证流程均未改动。

## 使用说明

Cloudflare 验证由网站控制，应用无法绕过。本次只调整阅读器自身的滚动与位置恢复行为，不会增加隐藏章节加载或额外验证请求。

## 安装

直接下载下方对应 APK 安装即可。Debug 和 Release 使用相同签名，可直接覆盖安装，不需要先卸载旧版本。

- `debug`：用于测试，保留调试构建标识。
- `release`：正式使用，推荐下载。
